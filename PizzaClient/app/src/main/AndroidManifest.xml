<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdproject.szabi.pizzaclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_food"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_food"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/StartupTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.home.HomeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.detailedprod.ProductActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.shoppingcart.ShoppingCartActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.address.AddressActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.orders.OrdersActivity"
            android:screenOrientation="portrait" />

        <service android:name=".ui.OrderUpdateService" />
    </application>

</manifest>